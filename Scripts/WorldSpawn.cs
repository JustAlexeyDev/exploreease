using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WorldSpawn : MonoBehaviour
{
    public GameObject[] blocks;
    // Start is called before the first frame update
    void Start()
    {
        if (gameObject.tag == "SpawnBlock1"){
           
        }    
        if (gameObject.tag == "SpawnBlock2"){

        }    
        if (gameObject.tag == "SpawnBlock3"){

        }    
        if (gameObject.tag == "SpawnBlock4"){

        }    
        if (gameObject.tag == "SpawnBlock5"){

        }    
        if (gameObject.tag == "SpawnBlock6"){

        }    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
